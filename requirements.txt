numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
# For the real reaction dataset (--dataset buchwald):
pandas>=2.0
openpyxl>=3.1
# LLM agent backends (only the one you use is needed):
google-genai>=1.0      # --agent gemini
anthropic>=0.40        # --agent claude
# Dev/tests:
pytest>=7.0
